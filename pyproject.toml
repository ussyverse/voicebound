[project]
name = "voicebound"
version = "0.1.0"
description = "The Last Broadcast: an AssemblyAI, Luna and OmniVoice mystery"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.30,<1", "httpx>=0.27,<1", "python-multipart>=0.0.18", "python-dotenv>=1,<2"]
[dependency-groups]
dev = ["pytest>=8,<10", "pytest-asyncio>=0.24,<2"]
[tool.pytest.ini_options]
asyncio_mode = "auto"
